# PCL.Mac 项目优化与企业级改造方案

## 1. 文档目的

本文档基于对 PCL.Mac 当前工程结构、核心页面组织方式、状态管理方式、异步任务使用方式、性能与交互实现方式的审查，给出一份面向企业级产品演进的系统性优化方案。目标不是推翻现有实现，而是在保留当前可用基础的前提下，逐步将项目从“功能可用型工程”提升为“可维护、可扩展、可测试、可观测、可协作”的企业级客户端产品。

本文重点覆盖六个方面：整体架构规范、内存与资源优化、交互体验优化、性能与稳定性优化、工程治理与质量体系、分阶段落地路线图。

## 2. 项目现状概述

从当前代码结构看，项目已经形成了较明确的基础分层：`PCL.Mac` 负责主应用与界面层，`PCL.Mac.Core` 承担较多核心能力与通用逻辑，另有测试 target 与 CI 配置，说明项目已经具备持续演进基础。

当前技术栈与组织特征主要表现为以下几点：一是应用基于 SwiftUI 构建 macOS 客户端；二是项目采用 App、Views、ViewModels、Services、Managers、Components 这样的目录划分；三是核心业务如实例管理、下载、联机、账号、更新、日志等功能已经形成可运行链路；四是部分能力已具备缓存、异步任务、测试、日志等工程意识。

这些基础说明项目并非原型代码，而是已经进入长期维护阶段。但从企业级产品标准衡量，目前仍然存在几个核心短板：全局状态和单例偏多、路由和业务耦合较重、超大文件和复杂页面开始累积、任务生命周期治理不足、交互状态缺乏统一规范、生产稳定性兜底不够完善、可测试性和可观测性仍偏弱。

## 3. 总体判断

PCL.Mac 当前更接近“完成度较高的个人或小团队产品工程”，还未达到“企业级客户端产品架构”的成熟度。它的主要问题不在于某个算法或某个页面写得差，而在于整体演进方式更偏向功能堆叠：随着功能增加，状态、路由、页面和业务逻辑逐步缠绕在一起，导致后续维护成本会以非线性方式上升。

如果继续沿用当前模式开发，短期内可以继续交付功能，但中长期会面临以下风险：页面修改牵一发动全身、多人协作冲突增多、线上问题定位变慢、性能问题难以归因、测试难覆盖关键路径、版本迭代速度逐步下降。

因此，本次优化的核心目标不是单纯“提速”或“省内存”，而是建立一套支持长期迭代的产品底座。

## 4. 核心问题诊断

### 4.1 架构层问题

当前最明显的架构问题是路由层承担了过多职责。`AppRouter.swift` 不仅维护导航状态，还直接负责页面装配，并通过超长 `switch` 组织大量页面内容和侧边栏逻辑。这使路由层同时承担了导航中心、页面工厂、业务分发器三种职责，后续任何页面扩展都可能继续推高耦合度。

此外，当前路由枚举中存在将业务对象直接塞入路由参数的情况，例如传递 `MinecraftRepository` 或完整的 `ProjectListItemModel`。这种做法会导致导航层与业务对象生命周期耦合，增加状态复制、数据不一致和测试困难的风险。企业级产品中，路由通常只负责传递轻量参数，例如 id、tab、type、filter 等，而不会直接传递完整业务实体。

第二个问题是全局单例数量较多。当前项目中多个模块通过 `static let shared` 的方式暴露全局实例，包括路由、任务管理、消息弹窗、更新、实例管理、Hint 管理等。该模式在单人开发阶段效率很高，但在企业级场景下会带来状态来源不透明、依赖边界不清晰、测试替身难注入、并发访问语义不稳定等问题。

第三个问题是模块边界仍然偏弱。虽然目录分层上已经有 Views、ViewModels、Services、Managers，但这些概念在职责上尚未完全稳定。例如部分 View 中仍承担较重的文件系统操作、任务调度、业务状态切换；部分 ViewModel 直接操作全局配置或调用弹窗；部分 Manager 同时承载状态、流程控制和 UI 协调。这会让系统逐渐走向“表面分层、实质耦合”。

第四个问题是大文件聚集。`InstanceExtraPages.swift` 已经成为典型超大文件，不仅负责界面呈现，还负责文件浏览、导入、删除、备份、排序、状态反馈等多类职责。类似文件在企业项目中通常会迅速演变成维护热点和冲突热点。

### 4.2 内存与资源使用问题

当前项目已经在部分场景下使用了 `NSCache`，例如皮肤数据缓存与目录大小缓存，这说明项目具备一定资源优化意识。但整体上仍缺少统一的缓存治理策略，包括缓存命名规范、容量分层、失效策略、手动清理入口、内存压力响应和命中率监控。

异步任务生命周期也是一个重要隐患。多个模块使用 `Task` 或 `Task.detached` 启动异步任务，但并未建立一致的取消、回收、重入保护和页面退出清理机制。随着页面切换、重复加载、轮询任务和后台请求增多，容易出现请求未及时取消、状态回写错位、无主任务长期驻留等问题。这类问题未必会立即表现为明显泄漏，但会逐步演变为内存占用增长、无效计算和偶发状态异常。

另一个风险来自根视图与全局状态绑定过多。`ContentView` 同时观察多个全局对象，当全局 overlay、Hint、任务状态、启动状态发生变化时，根视图刷新面会被放大。随着功能增长，这种结构更容易出现“局部状态变化触发大范围刷新”的性能和内存浪费。

### 4.3 交互体验问题

当前产品已经具备较丰富的动画、按钮反馈和局部提示机制，视觉上有一定活力。但整体交互规范仍不统一，主要表现为：不同组件使用不同动画时长与节奏；加载、失败、空态、重试等状态没有形成统一模型；搜索、翻页、弹窗、提示等交互反馈以功能实现为主，缺少企业级产品所要求的一致性和预期稳定性。

例如搜索页每次查询时会清空原有结果并回到加载态，这在用户连续搜索或翻页时容易造成闪烁和中断感。更成熟的交互方式应当区分首次加载、增量刷新、局部加载、失败回退和空结果展示，而不是统一重置整个页面内容。

列表选择状态目前仍存在基于 index 管理的情况，这在排序、筛选、插入删除时容易出现选择态错位。企业产品通常要求交互状态尽量与稳定业务标识绑定，而不是依赖数组位置。

### 4.4 性能与稳定性问题

性能方面，当前最大的风险并不在于单一慢函数，而在于页面状态、全局刷新和异步任务的叠加效应。超长 `switch`、大视图 body、多个全局观察对象、重复创建 task、频繁重置列表内容，都会在中大型数据量、频繁切页和复杂用户操作下放大开销。

稳定性方面，当前代码中仍存在 `try!`、`fatalError` 等生产路径风险点。这类实现对个人项目来说可以接受，但企业产品不能依赖“默认不会出错”的假设。生产环境必须优先保证不崩溃、可回退、可记录、可恢复。

启动流程也偏重。应用启动阶段集中进行了目录初始化、日志初始化、缓存读取、字体加载、版本缓存、远程拉取、窗口创建、更新检查等操作。虽然现在仍可运行，但从企业级产品视角看，启动职责需要更精细地拆分成可观测的 bootstrap pipeline，以便后续控制首屏时延和定位启动异常。

### 4.5 工程治理问题

从仓库结构和 CI 文件看，项目已经具备一定工程化基础，但距离成熟企业研发体系仍有明显差距。当前尚未看到完整的模块边界约束、依赖方向治理、统一编码规范、可量化的质量门禁、覆盖核心场景的自动化测试矩阵、稳定的可观测性策略和标准化的重构流程。

如果没有这些机制，后续即使完成一次重构，也很容易随着多人协作再次回到“功能优先、结构退化”的状态。

## 5. 企业级目标架构建议

建议项目未来演进为五层结构。

第一层是 App 层，负责应用启动、窗口生命周期、导航容器、依赖装配和全局环境注入。

第二层是 Feature 层，按业务域拆分模块，例如 Launch、Download、Instance、Account、Multiplayer、Settings、Update。每个模块内部自带 View、ViewModel、UseCase、State、RouteMapper、Service 协调代码。

第三层是 Domain 层，沉淀业务实体、协议、用例接口、状态模型，不依赖 SwiftUI 或 AppKit。

第四层是 Infrastructure 层，负责网络、文件系统、下载、日志、缓存、认证、外部进程、第三方 SDK 适配。

第五层是 Shared 层，沉淀设计系统、基础组件、主题、通用状态组件、工具函数和跨模块 UI 规范。

在这个结构中，需要明确几条基本约束。第一，路由只传轻量参数，不传业务大对象。第二，View 不直接承担重 IO 和复杂业务流程。第三，ViewModel 不直接依赖大量全局 shared，而应通过协议或依赖容器注入。第四，Manager 与 Service 的职责需要被严格区分，前者更偏进程内协调或全局状态，后者更偏能力提供和流程执行。第五，Core 不应持续扩张为“所有通用代码堆放区”，而应拆解成 Domain 与 Infrastructure 的明确边界。

## 6. 详细优化方案

### 6.1 架构规范优化

第一项工作是重构路由层。建议将 `AppRouter` 从“路由 + 页面工厂 + 部分业务分发”的综合体收敛为纯导航状态容器。页面装配逻辑应逐步迁移到对应 feature module 的工厂或 coordinator 中。对于实例详情、资源下载、项目安装等场景，路由只传 `id` 或最小必要参数，具体业务数据由目标页面在模块内部自行加载或从共享状态中获取。

第二项工作是治理单例。建议采用“保留现状实现、先抽象协议、再引入依赖注入”的渐进路线。第一步，不急于删除 shared，而是先为关键全局能力定义协议，例如日志、弹窗、任务调度、更新、账号服务、下载服务。第二步，在 ViewModel 层开始面向协议编程。第三步，通过依赖容器统一装配默认实现。这样既不影响当前功能，又能为测试和模块解耦创造条件。

第三项工作是拆分超大文件。优先级最高的拆分对象是 `InstanceExtraPages.swift` 与 `AppRouter.swift`。前者建议按功能拆为多个子页面和配套 use case，例如 Mods、Saves、ResourcePacks、Screenshots、Servers 各自独立；后者建议拆为 Route 定义、Navigation State、Content Resolver、Sidebar Resolver 四部分。

第四项工作是统一职责命名。建议重新定义项目中的术语边界：View 用于呈现和交互绑定；ViewModel 用于页面状态编排；UseCase 用于业务动作组织；Service 用于能力调用；Repository 用于数据源获取和持久化；Manager 仅保留真正需要全局生命周期协调的对象。通过这种方式，目录不只是看起来分层，而是具备清晰含义。

### 6.2 内存优化方案

建议建立统一的异步任务治理规范。所有由页面或 ViewModel 发起的异步任务，都需要具备唯一生命周期归属、可取消句柄、退出时清理逻辑和重复触发保护。应尽量减少无约束的 `Task.detached` 使用，除非确有必要脱离当前 actor 或上下文。长轮询任务必须具备页面退出自动停止能力。

建议建立统一缓存体系。将现有 `NSCache` 用法升级为规范化缓存组件，明确缓存类型、缓存 key、容量策略、失效策略和刷新机制。对目录大小、皮肤数据、版本信息、搜索结果等高频读取项进行分级缓存设计。必要时补充磁盘缓存层，并为用户提供缓存清理入口。

建议减少根级观察对象的刷新影响。对于全局提示、弹窗、任务悬浮层等状态，应通过更细粒度的展示容器与局部状态更新机制，降低整棵视图树的重绘概率。

建议对高频列表与图片加载场景做进一步治理。皮肤、资源图标、目录项等应考虑预取、缩略图缓存、惰性解码与统一图片加载策略，避免在滚动列表中反复解码和主线程抖动。

### 6.3 交互优化方案

建议建立一套统一的交互状态规范。所有页面都应统一使用以下状态模型：初始化、加载中、加载成功、空结果、加载失败、重试中、局部更新中。不要让每个页面自行决定何时清空内容、何时弹 Hint、何时展示骨架或 loading 动画。

建议统一动画系统。将当前散落在组件中的动画时长、曲线、过渡方式收敛成设计令牌，例如快速反馈动画、中等切换动画、页面入场动画、危险操作反馈动画等。这样可以让产品交互风格更稳定，也便于性能调优。

建议优化搜索与分页体验。搜索场景应增加防抖、取消前序请求、保留旧结果直到新结果到达、失败时局部提示和内联重试。分页场景应增强边界状态、禁用态和加载反馈，避免出现“内容清空再重载”的闪烁体验。

建议统一反馈渠道。当前 Hint、消息框、按钮态、页面 loading 都是分散实现，后续应明确使用原则：轻提示用于低风险即时反馈，模态框用于中高风险确认和阻断，页面内联反馈用于失败恢复，状态栏或悬浮层用于持续性后台任务。

建议补充企业产品常见的体验要素，包括空态插画或说明、失败可恢复文案、一致的危险操作确认、标准化键盘快捷键支持、可访问性与视觉对比规范、统一的文案语气和术语表。

### 6.4 性能与稳定性优化方案

建议立即清理生产路径中的高风险语句，包括 `try!`、`fatalError`、强假设分支和未兜底的解包。开发期可保留 `assertionFailure` 或 debug-only 检查，但生产环境必须回退到可恢复错误流。

建议建立启动流程分段机制。将启动过程拆为基础环境初始化、配置恢复、缓存恢复、远程元数据准备、界面创建、非关键后台预热几个阶段，并为每个阶段记录耗时和异常。首屏优先级应高于所有非关键后台动作。

建议对高频页面建立性能基线。至少对实例列表、资源搜索页、下载页、联机页、设置页这几个核心路径进行手动和自动基准观测，关注首屏渲染时间、滚动流畅度、重复搜索开销、切页响应时间和内存峰值。

建议建立统一错误模型。当前大量错误直接拼接文案展示，后续应引入可归类的错误结构，例如网络错误、认证错误、文件系统错误、配置错误、第三方服务错误、未知错误，并明确对用户显示文案与对日志记录内容的分层。

建议增强诊断能力。企业产品应支持日志导出、关键环境信息采集、任务上下文记录、失败事件链路追踪和问题复现辅助信息。这样一旦出现线上复杂问题，可以更快定位问题来源。

### 6.5 工程规范与质量体系优化

建议建立模块依赖约束。通过文档和自动化检查明确哪些模块可以依赖哪些层，避免 UI 反向依赖基础设施细节，避免 feature 之间任意横向耦合。

建议补齐测试策略。当前虽然有测试 target，但后续应明确三层测试体系：纯业务逻辑使用单元测试，跨模块流程使用集成测试，关键用户路径使用 UI/冒烟测试。优先覆盖登录、实例加载、搜索下载、联机建立、更新流程等关键链路。

建议建立代码规范与重构规范。包括命名规则、文件大小警戒线、View/VM 职责边界、异步任务写法约束、错误处理规范、日志打印规范、缓存策略约束、PR 模板和代码评审清单。

建议建设可观测性。企业产品不能只依赖本地日志，应逐步具备关键事件埋点、崩溃统计、性能指标采集、用户关键动作路径监控和版本质量反馈体系。

建议在 CI 中加入更多质量门禁，包括构建、单元测试、格式检查、静态分析、关键模块覆盖率、必要时的性能回归检查。

## 7. 分阶段落地路线图

### 第一阶段：稳定性止血与底座治理

第一阶段目标是在不改变核心功能体验的前提下，优先清理最容易演变成线上事故或后续重构阻碍的问题。该阶段建议优先完成以下工作：清理生产路径中的 `try!` 和 `fatalError`；建立统一错误模型；梳理异步任务取消和生命周期规范；为启动流程打点并拆分关键步骤；建立日志导出与基础诊断能力。

这一阶段的收益是立刻提升稳定性，并为后续结构优化建立安全边界。

### 第二阶段：架构收敛与模块化改造

第二阶段建议聚焦最关键的结构热点，包括 `AppRouter`、`InstanceExtraPages`、全局 singletons 依赖、下载与实例管理的模块边界。此阶段应开始推进路由轻量化、特性模块拆分、依赖注入雏形和 feature 内部工厂化装配。

这一阶段的收益是显著改善可维护性，降低后续新增功能时的结构负担。

### 第三阶段：性能与交互统一治理

第三阶段应以用户感知为中心，对搜索、分页、加载、失败、重试、动画、滚动列表、全局提示与任务反馈等体验进行统一规范和性能调优。此阶段也建议建立若干核心页面的性能基线和回归检查方式。

这一阶段的收益是提升产品的一致性和使用质感，使其从“能用”迈向“成熟”。

### 第四阶段：企业化能力补齐

第四阶段建议完善埋点、特性开关、崩溃采集、远程配置、权限与隐私弹窗规范、测试矩阵、团队协作约束和发布质量门禁。这部分工作虽然不直接体现在某个页面上，但决定了产品能否长期稳定地以企业级方式演进。

## 8. 优先级建议

如果资源有限，建议按照如下优先级推进。

最高优先级应放在生产稳定性和架构阻塞点上，包括：清理强崩溃点、规范异步任务生命周期、降低全局 shared 耦合、拆分超大文件和重路由层。

中高优先级应放在高频用户路径体验上，包括：资源搜索体验、实例管理页面、列表与分页体验、加载与失败状态统一化。

中优先级应放在工程化能力建设上，包括：依赖注入、模块边界文档、测试体系、日志导出、质量门禁。

中长期优先级则是企业级平台能力，例如埋点、远程配置、特性开关和版本质量观测。

## 9. 关键改造对象建议

从当前代码审查结果看，建议优先聚焦以下关键文件和区域。

`PCL.Mac/App/AppRouter.swift` 是当前最核心的结构耦合点，建议优先改造。

`PCL.Mac/Views/Launch/InstanceSettings/InstanceExtraPages.swift` 是典型的大文件与多职责聚集区，建议尽快拆分。

`PCL.Mac/ViewModels/MultiplayerViewModel.swift` 包含较多异步任务与状态转换逻辑，建议重点治理并发与生命周期。

`PCL.Mac/ContentView.swift` 是全局状态与 overlay 聚合点，建议关注刷新范围和根视图职责。

`PCL.Mac/App/AppDelegate.swift` 是启动职责集中区，建议逐步拆出启动 pipeline。

`PCL.Mac.Core/Managers/LogManager.swift` 是稳定性与诊断能力的基础设施重点，应尽快提升健壮性。

`PCL.Mac/ViewModels/ResourcesSearchViewModel.swift` 与 `PCL.Mac/Views/Download/ResourcesSearchPage.swift` 是交互与性能统一治理的良好切入点。

## 10. 预期收益

完成本方案后，项目预期将获得以下收益：首先，架构边界更稳定，后续新功能开发和多人协作成本显著降低；其次，异步任务、缓存和状态刷新更可控，性能与内存表现更稳定；再次，用户交互体验会更统一，产品质感更接近成熟商业客户端；最后，项目将具备更强的测试能力、诊断能力和质量治理能力，为长期发布和持续演进奠定基础。

## 11. 结论

PCL.Mac 当前具备良好的产品基础与继续优化的价值，但若目标是“企业级产品”，则必须从以功能为中心的演进方式，转向以架构治理、状态治理、稳定性治理和工程体系治理为中心的演进方式。

本项目不需要推倒重来，更适合采取渐进式重构路线：先止血、再收敛、再统一、再平台化。只要按阶段推进，并把高频问题文件作为第一批重点对象处理，项目完全有机会在保留现有成果的同时，逐步演进为结构清晰、性能稳定、体验一致、适合长期协作的企业级 macOS 产品。
